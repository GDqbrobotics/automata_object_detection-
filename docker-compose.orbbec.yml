services:
  mqtt:
    image: eclipse-mosquitto:latest
    ports:
      - 1883:1883
      - 9001:9001
      - 8080:8080
    volumes:
      - ./mqtt/mosquitto-config:/mosquitto/config/
      - ./mqtt/auth:/auth 
  automata-cv:
    build:
      context: .
      dockerfile: Dockerfile.orbbec
    privileged: true
    network_mode: host
    volumes:
      - .:/home/workspace
      - /tmp/.X11-unix:/tmp/.X11-unix
      - /dev:/dev
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    environment:
      - DISPLAY=$DISPLAY  # needed to show results on screen
      - XAUTHORITY=$XAUTHORITY  # needed to show results on screen
      - PYTHONUNBUFFERED=1  # show Python logs in real time (no stdout buffering)
    command: bash -c "source /opt/venv/bin/activate && python3 /home/workspace/object_extraction.py --camera-type orbbec --stream-width 1280 --stream-height 720 --mqtt-host 127.0.0.1"
